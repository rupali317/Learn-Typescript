type Point = {
  x: number;
  y: number;
};

const point_obj = { x: 0, y: 0, z: 0 };

const pt: Point = { x: 0, y: 0 };

console.log(pt);

type userID = number;

let id: userID = 3;

console.log(id);
